import { RepeatTagDirective } from './repeat-tag.directive';

describe('RepeatTagDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatTagDirective();
    expect(directive).toBeTruthy();
  });
});
